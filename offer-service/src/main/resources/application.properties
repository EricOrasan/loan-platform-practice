spring.application.name=offer-service

server.port=${SERVER_PORT:8084}

# Database
spring.datasource.url=jdbc:postgresql://localhost:5432/offer_db
spring.datasource.username=postgres
spring.datasource.password=${POSTGRES_PASSWORD}

# JPA
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false
spring.jpa.show-sql=true

# Liquibase
spring.liquibase.change-log=classpath:/db/changelog/db.changelog-master.yaml

# Kafka
spring.kafka.bootstrap-servers=${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}

# Consumer
spring.kafka.consumer.group-id=${KAFKA_CONSUMER_GROUP_ID:offer-service}
spring.kafka.consumer.auto-offset-reset=earliest

# Producer
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JacksonJsonSerializer
spring.kafka.producer.acks=all
spring.kafka.producer.properties[enable.idempotence]=true

# Do not couple events to Java class names through Kafka headers
spring.kafka.producer.properties[spring.json.add.type.headers]=false

# Topics
app.kafka.topics.loan-assessment-completed=${LOAN_ASSESSMENT_COMPLETED_TOPIC:loan.assessment.completed}
app.kafka.topics.loan-offer-generated=${LOAN_OFFER_GENERATED_TOPIC:loan.offer.generated}